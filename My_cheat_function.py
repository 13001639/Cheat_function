def cheat(string):
    if string == 'Q2.2.4':
        return ("First, define special vector creating array with vector[0], then with a for loop in range of the length of the vector, if i of vector is not in the special vector, append the i to it.")
    elif string == 'Q2.2.1':
        return ("Import datetime, define current hour, minute and time by adding them. if current time is in the range (use if _ and _:), print required message.")
    else:
        return ("Sorry, not in my definition")
